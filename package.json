{
  "name": "@codeaholicguy/cv",
  "description": "My CV built with Gatsby",
  "version": "1.0.0",
  "author": "hoang@codeaholicguy.com",
  "dependencies": {
    "gatsby": "^5.16.1",
    "gatsby-plugin-react-helmet": "^6.16.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-helmet": "^6.1.0",
    "styled-components": "^4.3.2"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write \"src/**/*.js\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gatsby build --prefix-paths && cp CNAME public/CNAME && gh-pages -d public"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "prettier": "^1.18.2"
  }
}
